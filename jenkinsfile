pipeline {
  agent any {
   tools { 
        maven 'Maven 3.8.7' 
        jdk 'jdk7' 
    }
   stages {
   
      stage('Build') {
                   sh 'mvn -Dmaven.test.failure.ignore=true install' 

      }
      stage('Test'){
       sh 'mvn clean package'
      }
    }
  }
}
