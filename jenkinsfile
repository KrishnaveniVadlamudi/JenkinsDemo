pipeline {
  agent any {
   tools { 
        Maven 'MAVEN3.8.7' 
        JDK 'JAVA1.7' 
    }
   stages {
   
      stage('Build') {
                   sh 'mvn -Dmaven.test.failure.ignore=true install' 

      }
      stage('Test'){
       sh 'mvn clean package'
      }
    }
  }
}
